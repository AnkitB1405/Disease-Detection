"""FastAPI backend package for the Crop Disease Detection app.

Wraps the pure ML/business packages (crop_detection/, chatbot/, tracking/)
behind a REST + SSE + WebSocket API consumed by the Flutter client.
"""
