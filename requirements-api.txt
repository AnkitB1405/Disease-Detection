# FastAPI backend (Flutter client). Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-api.txt
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9
websockets>=12.0
